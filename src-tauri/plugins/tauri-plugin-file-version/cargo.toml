[package]
name = "tauri-plugin-file-version"
version = "0.0.1"
authors = [ "Tauri Programme within The Commons Conservancy" ]
description = "A Tauri plugin for reading a files version."
edition = "2021"
exclude = ["/examples", "/webview-dist", "/webview-src", "node_modules"]
license = "Apache-2.0 OR MIT"

[dependencies]
tauri = "1"
version_info = "0.0.5"

[features]